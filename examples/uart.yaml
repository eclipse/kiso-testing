auxiliaries:
  DUT:
    connectors:
        com: uart
    config: null
    type: pykiso.lib.auxiliaries.example_test_auxiliary:ExampleAuxiliary
connectors:
  uart:
    config:
      serialPort: COM3
      type: pykiso.lib.auxiliaries.cc_uart:CCUart
test_suite_list:
- suite_dir: test_suite_1
  test_filter_pattern: '*.py'
  test_suite_id: 1
